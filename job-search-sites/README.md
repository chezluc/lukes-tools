# Job Search Sites

This folder contains a plain-text list of sites I use to search for jobs (`subdomains.txt`).

## Format

- One entry per line.
- Keep it ASCII and lowercase when possible.
- Prefer domains or full URLs, but stay consistent.

## Notes

- This is just data; any validation, filtering, or scraping should happen in a separate tool.
